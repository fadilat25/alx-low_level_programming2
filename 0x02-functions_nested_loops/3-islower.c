#include "main.h"
/**
 *_islower - checks if the character 'c' is a lowercase character
 *
 *Return: returns a value based on preset conditions
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
