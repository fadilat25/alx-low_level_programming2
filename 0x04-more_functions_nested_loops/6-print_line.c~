#include "main.h"
/**
 * print_line - prints a straight line
 *@n: the number of dashes
 */
void print_line(void)
{
int line;
if (n > 0)
{
for (line = 0; line < n; line++)
_putchar('_');
}
_putchar('\n');
}
